logging:
  level:
    root: INFO
  file:
    name: ./log/company_marketplace.log

spring:
  application:
    name: marketplace
  datasource:
    url: "jdbc:h2:./db;INIT=RUNSCRIPT FROM 'classpath:sql/createSchema.sql'"
    username: "sa"
    password: ""
    driver-class-name: "org.h2.Driver"
  h2:
    console:
      enabled: true
server:
  port: 8080
  error:
    include-message: always
